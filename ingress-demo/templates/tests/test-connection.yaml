apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingress-demo.fullname" . }}-test-connection"
  labels:
    {{- include "ingress-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingress-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
